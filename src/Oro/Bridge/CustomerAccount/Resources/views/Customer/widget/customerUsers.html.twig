{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

<div class="widget-content">
    {% set scope = 'account-customer-%customer%'|replace({'%customer%': customer.id}) %}

    {{ dataGrid.renderGrid(
        oro_datagrid_build_fullname('account-user-by-account-grid', scope),
        {account_id: customer.id})
    }}
</div>
