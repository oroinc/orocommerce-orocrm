oro:
    workflow:
        quote_flow:
            label: 'Quote flow'
            step:
                open:
                    label: 'Open'
                quote_sent:
                    label: 'Quote Sent'
            attribute:
                status:
                    label: 'Status'
                quote:
                    label: 'Quote'
                customer:
                    label: 'Customer'
            transition:
                quote_send:
                      label: 'To Quote Sent Step'
                quote_create:
                      label: 'Create Quote'
                quote_add:
                      label: 'Create Quote'
